info:
  name: Investor portfolio (OWNER or ADMIN)
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}portfolio/investors/{{exampleInvestorId}}"
  auth:
    type: bearer
    token: "{{ownerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
