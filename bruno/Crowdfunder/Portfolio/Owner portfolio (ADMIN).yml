info:
  name: Owner portfolio (ADMIN)
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}portfolio/owners/{{exampleOwnerId}}"
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
