info:
  name: My portfolio
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}portfolio/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
