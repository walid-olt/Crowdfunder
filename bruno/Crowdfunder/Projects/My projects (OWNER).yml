info:
  name: My projects (OWNER)
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}projects/mine"
  auth:
    type: bearer
    token: "{{ownerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
