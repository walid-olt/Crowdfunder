info:
  name: Close project (OWNER)
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}projects/{{exampleProjectId}}/close"
  auth:
    type: bearer
    token: "{{ownerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
