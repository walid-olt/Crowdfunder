info:
  name: Open projects
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}projects/open"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
