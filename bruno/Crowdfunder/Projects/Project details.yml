info:
  name: Project details
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}projects/{{exampleProjectId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
