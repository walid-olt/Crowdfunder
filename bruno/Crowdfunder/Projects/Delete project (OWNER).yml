info:
  name: Delete project (OWNER)
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{baseUrl}}projects/{{exampleProjectId}}"
  auth:
    type: bearer
    token: "{{ownerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
