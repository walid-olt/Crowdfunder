info:
  name: Invest in project (INVESTOR)
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}projects/{{exampleProjectId}}/invest"
  body:
    type: json
    data: |
      {
        "amount": 1000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
