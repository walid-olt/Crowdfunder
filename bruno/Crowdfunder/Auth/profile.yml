info:
  name: profile
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}auth/me"
  auth:
    type: bearer
    token: "{{ownerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
